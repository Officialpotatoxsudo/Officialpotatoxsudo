name: Update self-hosted cards

on:
  schedule:
    - cron: "0 0 * * 0" # weekly, Sunday 00:00 UTC
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - ".github/scripts/update_stats.py"
      - ".github/scripts/update_cards.py"
      - ".github/scripts/update_activity.py"
      - ".github/workflows/stats.yml"

permissions:
  contents: write

jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Regenerate cards
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python .github/scripts/update_stats.py
          python .github/scripts/update_cards.py
          python .github/scripts/update_activity.py
      - name: Commit if changed
        run: |
          if git diff --quiet -- stats.svg activity.svg repo-openclaw.svg repo-nist_update.svg repo-slapfx_release.svg; then
            echo "cards unchanged"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add stats.svg activity.svg repo-openclaw.svg repo-nist_update.svg repo-slapfx_release.svg
          git commit -m "chore: refresh self-hosted cards"
          git push
